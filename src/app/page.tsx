import Intro from "@/components/Intro";
const Home = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center">
      <Intro />
    </div>
  );
};

export default Home;
